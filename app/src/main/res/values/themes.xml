<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FoodDelivery" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/orange_primary</item>
        <item name="android:colorPrimaryDark">@color/orange_variant</item>
        <item name="android:colorAccent">@color/orange_primary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/orange_primary</item>
    </style>
</resources>
